import 'package:flutter/material.dart';

class newshot extends StatelessWidget {
  const newshot({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: const Text("News and hot screnn"),
      ),
    );
  }
}
